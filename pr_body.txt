🎯 What: Added tests for `QuantumCanvas.tsx`.
📊 Coverage: Added tests to ensure `QuantumCanvas` properly renders and delegates props to the mocked `@react-three/fiber` and `@react-three/drei` components (Canvas, Stars, OrbitControls, ambientLight, pointLight).
✨ Result: `QuantumCanvas` is now fully tested under Node's native test runner despite `@react-three/fiber` module mocking complexities.
