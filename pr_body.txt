🎯 **What:** The `reducer` function and `initialState` within `context/MeditationContext.tsx` were modified to be exported for isolated testing. A new test file `tests/meditation-context.test.ts` was added to test the pure `reducer` logic directly without the overhead of rendering a Context Provider.

📊 **Coverage:** The new test suite covers all actions handled by the `reducer`:
- `ADD_SESSION`: Verifies payload is appended to `sessions` array.
- `UPDATE_PROGRESS`: Verifies `progress` state reflects payload.
- `SET_CONSCIOUSNESS_LEVEL`: Verifies `consciousnessLevel` state reflects payload.
- `default`: Verifies unknown actions return the unmutated `initialState` (reference equality).

✨ **Result:** Test coverage significantly improved for the `MeditationContext`. State transitions are now validated predictably, preventing future regressions.
